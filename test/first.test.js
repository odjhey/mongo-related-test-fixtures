const { expect } = require("chai");
describe("the first test", () => {
  it("should: case 1", () => {
    // arrange
    const given = {};
    // act

    // assert
    expect(true).to.be.true;
  });
});
